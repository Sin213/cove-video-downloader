# Cove Video Downloader — Python backend dependencies.
#
# The backend uses only stdlib (urllib, subprocess, threading, json, ssl).
# Modern Python on Linux/Windows finds system CA bundles automatically,
# so we no longer vendor certifi. If you hit SSL verification errors on
# an ancient platform, install certifi and ssl_context.py will pick it up.
